/**
 * Future API-backed repositories go here.
 *
 * Example swap in dataService.ts:
 * ```
 * import { ApiContentRepository } from './repositories/api/contentRepository'
 * import { ApiProgressRepository } from './repositories/api/progressRepository'
 *
 * const useApi = import.meta.env.VITE_USE_API === 'true'
 * const repositories = useApi ? createApiRepositories() : createLocalRepositories()
 * ```
 */

export {}
